import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24414-datetime-active-day',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
